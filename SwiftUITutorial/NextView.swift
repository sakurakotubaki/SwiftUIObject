import SwiftUI

struct NextView: View {
    
    var body: some View {
        VStack {
            Text("次のページ")
        }
        .padding()
    }
}

struct NextView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
